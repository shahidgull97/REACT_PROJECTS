import React from "react";
import ReactDOM from "react-dom/client";

import IPodInterface from "./Ipad";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <IPodInterface />
  </React.StrictMode>
);
